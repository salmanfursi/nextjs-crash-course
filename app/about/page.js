import React from 'react';

const page = () => {
  // throw new Error("there was an huge error ")
  return (
    <div className="mt-20 text-3xl">
    About page 
    </div>
  );
};

export default page;